public enum DamageType
{
    Normal,
    Critical,
    Death,
}

public enum BehaviourType
{
    LMove,
    RMove,
    Jump,
    Flash,
}

public enum Rarity
{
    Common,
    Rare,
    Epic,
    Legendary
}